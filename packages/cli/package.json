{
  "name": "@agent-kit/cli",
  "version": "1.0.0",
  "description": "Agent Kit CLI tool",
  "main": "dist/index.mjs",
  "bin": {
    "agent-kit": "dist/index.mjs"
  },
  "scripts": {
    "build": "tsup",
    "clean": "rimraf dist",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-kit/ui": "workspace:*",
    "react": "^18.2.0",
    "ink": "^4.4.1",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "rimraf": "^5.0.0",
    "tsup": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
